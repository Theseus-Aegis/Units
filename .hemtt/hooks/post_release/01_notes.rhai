let version_short = `${HEMTT_PROJECT_VERSION_MAJOR}.${HEMTT_PROJECT_VERSION_MINOR}.${HEMTT_PROJECT_VERSION_PATCH}`;

print(`Notes:
      - Rename release archive to tac_units_${version_short}.zip!`);
